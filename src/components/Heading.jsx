// following line is necessary for every React component
// so it's possible to write JSX code
import React from "react";

function Heading() {
  return <h1>My Favourite Foods</h1>;
}

export default Heading;
// allows to export and use in other programs this component
